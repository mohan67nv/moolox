# CANONICAL MASTER FEATURE REGISTRY
## Historical 112-Feature Product Inventory plus 13 Assurance and 9 Final Pre-GA Trust Features
**Document Series:** Master Execution Plan (MEP) — File 2 of 8 | **Status:** Canonical Inventory | **Duplicates Merged:** 100%

> **Reconciled operation:** Use the three-digit IDs and semantic rules in `CANONICAL_RECONCILIATION.md`. This file's two-digit IDs are historical planning aliases only. Sprints 0–3 status rows are superseded by `docs/SPRINT_COMPLETION_LEDGER.md`. Legacy DIOS and `@dios/*` references mean Moolox and `@moolox/*` and must not be copied into new implementation.

> **Final inventory overlay (2026-07-15):** The canonical inventory is 134 features: 112 historical product features, 13 production-assurance features, and 9 final pre-GA trust/validation features. Existing module tables below remain preserved; the new definitions and sprint reassignments in the addendum at the end of this file are binding.

---

# INVENTORY STRUCTURE & METRIC SPECIFICATIONS

Every atomic feature across the entire 24-month vision of **Antigravity (DIOS)** is recorded here with exactly sixteen required execution fields. Duplicates across historical bibles (`Docs 1–7`) have been strictly merged into single canonical definitions (`Feature ID`).

### Complexity Scale & Effort Mapping
- **`XS` (Extra Small):** `< 2 Days Effort` — Schema extensions, config constants, or single API route handlers.
- **`S` (Small):** `3–5 Days Effort` — Standard React UI components, DB query models, or local linter rules.
- **`M` (Medium):** `1–2 Weeks Effort` — Core state synchronizers, AI prompt routers, or webhook bridges.
- **`L` (Large):** `2–4 Weeks Effort` — High-speed AST parsers, virtualized DOM engines, or edge compilers.
- **`XL` (Extra Large):** `4–6 Weeks Effort` — Web Worker sandboxes, CRDT WebSockets, or 3D WebXR viewports.

---

# MODULE 1: AUTHENTICATION (`@dios/auth`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`AUTH-01`** | **Clerk Enterprise Identity & JWKS Verification Router:** Core authentication gateway integrating Clerk middleware, verifying JWT signatures at edge (`< 1ms`), and injecting `sub` user claims onto Next.js request headers. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / None | **S** / `3 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / Authentication | **Platform Pod** — Clerk middleware mounted; invalid JWTs return 401; user `sub` validated in `< 2ms` on all `/api/*` routes. |
| **`AUTH-02`** | **Workspace Role-Based Access Control (`RBAC`) Engine:** Granular permission check matrix (`Owner`, `Admin`, `Editor`, `Viewer`) verified against `workspace_members` table on every protected API and tRPC procedure. | Doc 4 / Doc 5 | CRITICAL / CRITICAL | No / `AUTH-01`, `WS-01` | **M** / `6 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / Authentication | **Platform Pod** — tRPC middleware rejects non-members with `FORBIDDEN`; exact role matrix enforced across workspace operations. |
| **`AUTH-03`** | **Metered AI Credit Billing Gate & Circuit Breaker:** Real-time credit pool auditor (`1 credit = $0.01`) deducting credits per Sonnet/Vision prompt before generation and blocking execution when balance hits `0`. | Doc 5 / Doc 6 | CRITICAL / HIGH | No / `AUTH-01` | **M** / `5 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Authentication | **AI Pod** — Upstash Redis metered counter decrements atomically on prompt; returns `402 Payment Required` when depleted. |
| **`AUTH-007`** | **GitHub App Credential Authorization Bridge:** GitHub App installation callback and authorization flow securely storing installation credentials using versioned envelope encryption, binding least-privilege repository access to project context, and supporting revocation/suspension. | Reconciliation / Architecture Review | CRITICAL / CRITICAL | No / `AUTH-001` | **M** / `5 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Authentication | **Platform Pod** — User completes GitHub App installation; repository scope is explicit; credentials never enter logs; revocation immediately blocks jobs; `SEC-002` rotation hooks are preserved. |
| **`AUTH-004`** | **SAML 2.0 Single Sign-On (`SSO`) Enterprise Gateway:** Identity federation bridge allowing enterprise tenants to connect Okta, Azure AD, and Google Workspace SAML IdPs to their Moolox enterprise organization. | Doc 5 / Doc 6 | HIGH / CRITICAL | No / `AUTH-001`, `ENT-001`| **L** / `14 Days` | Preserved Hook / **Sprint 10** | **`Enterprise`** / Authentication | **Enterprise Pod** — SAML IdP initiated login redirects to workspace; exact domain matching auto-assigns employee roles. |
| **`AUTH-06`** | **Headless API Token & Scoped Key Generator:** Developer API key management console generating `dios_live_sk_*` tokens with granular read/write permission scopes (`read:ast`, `write:tokens`) for SDK usage. | Doc 4 / Doc 7 | HIGH / HIGH | No / `AUTH-02` | **S** / `4 Days` | Not Started / **Sprint 11** | **`Platform`** / Authentication | **Platform Pod** — SHA-256 hashed API keys stored in DB; SDK requests authenticated via `Authorization: Bearer` header. |

---

# MODULE 2: WORKSPACE (`@dios/workspace`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`WS-01`** | **Multi-Tenant Workspace Root & Slug Router:** Core tenant boundary model (`workspaces` table) establishing isolated workspace slugs (`dios.app/w/acme`) and storing credit quotas. | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `AUTH-01` | **S** / `3 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / Workspace | **Platform Pod** — User registration auto-creates default workspace; slug uniqueness enforced via DB constraint. |
| **`WS-02`** | **Workspace Member Invitation & Email Dispatcher:** Team onboarding flow generating secure, time-limited invitation links (`JWT 48hr TTL`), sending Resend transactional emails, and assigning member roles. | Doc 3 / Doc 5 | HIGH / HIGH | No / `WS-01`, `AUTH-02` | **M** / `6 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / Workspace | **Platform Pod** — Invitee clicks link, authenticates via Clerk, and is instantly appended to `workspace_members` with assigned role. |
| **`WS-03`** | **Workspace Settings & Brand Tone Metadata Store:** Global configuration dashboard allowing admins to set default brand fonts, primary hex accents, and natural language `brand_tone` instructions (`"Formal Fintech"`). | Doc 3 / Doc 4 | HIGH / MEDIUM | No / `WS-01` | **S** / `4 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / Workspace | **Platform Pod** — Settings saved in `workspaces.metadata`; AI generator automatically injects `brand_tone` into prompt context. |
| **`WS-04`** | **Multi-Workspace Switcher & Dashboard Matrix:** High-speed dashboard view (`/dashboard`) displaying all member workspaces, active project cards, recent AI generations, and quick action search bar. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `WS-01`, `PRJ-01` | **S** / `5 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Workspace | **Platform Pod** — Dropdown switches active `workspaceId` in `< 100ms`; dashboard renders all active projects with thumbnail previews. |
| **`WS-05`** | **Workspace Audit Trail & Activity Feed (`OTel`):** Immutable event log recording all critical member actions (`PROJECT_CREATED`, `TOKEN_MODIFIED`, `GIT_EXPORTED`) with actor ID, timestamp, and IP address. | Doc 5 / Doc 6 | HIGH / HIGH | No / `AUTH-02` | **M** / `6 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Workspace | **Platform Pod** — Activity feed renders chronological events; audit entries cannot be modified or deleted by standard admins. |
| **`WS-06`** | **Agency Client Portal & Handoff Permission Tier:** Specialized `Client Editor` role restricting external client members to editing text copy and uploading blog images without altering design token layout math. | Doc 5 / Doc 7 | HIGH / HIGH | No / `WS-02`, `AUTH-02` | **M** / `8 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Workspace | **Ecosystem Pod** — Client login hides Property Inspector layout controls; double-click opens restricted inline rich-text editor only. |

---

# MODULE 3: PROJECTS (`@dios/project`) — 7 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`PRJ-01`** | **Project Metadata Core & Version Pointer Model:** Project entity management (`projects` table) storing project name, slug, active GitHub repo URL, and pointer to `active_version_id` snapshot. | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `WS-01` | **S** / `3 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / Projects | **AST Pod** — Project creation generates unique ID and scaffolds initial empty `project_versions` row with default `tokens.json`. |
| **`PRJ-02`** | **Debounced Auto-Save Memory Buffer (`Zustand`):** High-speed browser memory store (`Zustand`) holding active canvas mutations locally and flushing Zstd-compressed JSONB to DB every 3 seconds of inactivity. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `PRJ-01`, `AST-01`| **M** / `6 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / Projects | **AST Pod** — Drag-and-drop modifies local state at 60fps; HTTP POST auto-flushes to `/api/project/save` after 3s debounce without lag. |
| **`PRJ-03`** | **Optimistic Single-Editor File Locking:** Collision prevention mechanism locking active `version_id` to single editor (`Last-write-wins at node level`) to prevent split-brain edits during `v0.5`. | Doc 6 / Doc 7 | HIGH / HIGH | No / `PRJ-02` | **S** / `4 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / Projects | **AST Pod** — Second editor attempting simultaneous save receives `409 Conflict` banner and prompt to reload latest version tree. |
| **`PRJ-04`** | **Instantaneous Undo/Redo Checkpoint History:** Non-destructive branching history allowing instant 1-click rollback of canvas mutations or AI generation turns (`Ctrl+Z / Ctrl+Y`) via version snapshots. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `PRJ-02` | **M** / `6 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / Projects | **AST Pod** — Undo stack stores up to 50 local snapshots in browser memory; `Ctrl+Z` instantly reverts AST tree and re-renders canvas. |
| **`PRJ-05`** | **Project Duplication & 1-Click Template Cloning:** High-speed cloning bridge copying entire `project_versions.ast_tree` and `tokens.json` to a new project ID inside the user's workspace in `< 2 seconds`. | Doc 3 / Doc 5 | CRITICAL / MEDIUM | No / `PRJ-01` | **S** / `3 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Projects | **AST Pod** — Clicking "Duplicate Project" spawns exact deep-copy with new slug (`project-name-copy`) and resets git pointers. |
| **`PRJ-06`** | **Asynchronous Visual Branching & Pull Requests:** Git-like visual branching (`design-exploration-v2`) allowing designers to experiment on isolated AST copies and merge back via side-by-side visual diff review. | Doc 4 / Doc 7 | HIGH / CRITICAL | No / `PRJ-01`, `AST-03`| **L** / `14 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Projects | **AST Pod** — User creates branch; edits do not affect main branch; clicking "Merge" runs sub-tree diffing and presents approval UI. |
| **`PRJ-07`** | **Spatial Project Workspace & 3D Canvas Root:** Extended project schema (`is_spatial = true`) configuring project runtime to render inside WebXR 3D spatial coordinate matrices (`Three.js / React Three Fiber`). | Doc 2 / Doc 7 | VISION / VISION | No / `PRJ-01` | **XL** / `20 Days` | Not Started / **Sprint 12** | **`Future`** / Projects | **Ecosystem Pod** — Project toggle enables 3D viewport canvas; AST nodes accept `position3D: [x, y, z]` vector properties. |

---

# MODULE 4: CANVAS (`@dios/canvas`) — 10 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`CNV-01`** | **React 19 Virtualized 60fps DOM Renderer:** High-performance viewport rendering engine (`@dios/canvas`) mounting clean React 19 components directly inside an isolated iframe (`Shadow DOM`). | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `AST-01` | **L** / `12 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Canvas | **Canvas Pod** — AST renders to DOM in `< 15ms`; element selection dragging maintains `60fps` frame rate with zero layout shifts. |
| **`CNV-02`** | **Bidirectional Property Inspector Panel (`Right Sidebar`):** Interactive right-hand sidebar mapped directly to selected `ASTNode.props` and `styles`. Editing padding (`p-6`) updates AST and re-renders canvas instantly. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `CNV-01`, `AST-03`| **M** / `8 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Canvas | **Canvas Pod** — Clicking DOM element highlights border and populates Property Inspector; dropdown/input edits reflect live in canvas. |
| **`CNV-03`** | **Multi-Viewport Matrix & Responsive Previewer:** Instant toggle bar switching between Mobile (`375px`), Tablet (`768px`), Desktop (`1440px`), and Ultra-Wide (`1920px`) viewports with side-by-side comparison. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `CNV-01` | **S** / `5 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Canvas | **Canvas Pod** — Viewport buttons resize iframe width instantly; media queries (`md:`, `lg:`) resolve accurately inside canvas window. |
| **`CNV-04`** | **Precision Bounding Box & Wireframe Mode (`Shift+W`):** Visual debugging layer rendering DOM margin outlines, flex/grid alignment vectors, and WCAG accessibility outline boundaries across the active page. | Doc 3 / Doc 6 | HIGH / MEDIUM | No / `CNV-01` | **S** / `4 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Canvas | **Canvas Pod** — Toggling `Shift+W` displays clean wireframe grid; hovering nodes displays box-model padding (`green`) and margin (`orange`). |
| **`CNV-05`** | **Interactive Breakpoint & Flex/Grid Layout Math:** Visual drag handles on canvas column splitters converting visual dragging directly into responsive Tailwind grid classes (`grid-cols-1 md:grid-cols-3`). | Doc 3 / Doc 4 | HIGH / HIGH | No / `CNV-02` | **M** / `8 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Canvas | **Canvas Pod** — Dragging column splitter updates `ASTNode.styles` grid definition; visual layout adjusts dynamically without CSS manual entry. |
| **`CNV-06`** | **Virtualized Sub-Tree Lazy Windowing (`100+ Pages`):** Memory optimization pruning off-screen DOM nodes on massive enterprise sites (`ASTNodeId windowing`) to maintain total browser memory usage under 150MB. | Doc 4 / Doc 6 | HIGH / CRITICAL | No / `CNV-01` | **L** / `10 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Canvas | **Canvas Pod** — Scrolling massive 500-node site unmounts off-screen DOM trees cleanly; memory profile confirms `< 150MB` heap. |
| **`CNV-07`** | **In-Canvas Sandpack / WebContainer Node Emulation:** In-browser runtime engine executing full Node.js server actions and API route mocks inside live canvas preview iframe (`@codesandbox/sandpack-react`). | Doc 4 / Doc 6 | HIGH / CRITICAL | No / `CNV-01`, `AST-02`| **XL** / `16 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Canvas | **Canvas Pod** — Complex dynamic forms and `fetch()` calls execute inside canvas iframe without throwing cross-origin or runtime errors. |
| **`CNV-08`** | **Multi-Cursor Presence & Live Avatar Highlights:** Visual collaboration layer rendering real-time colored user cursors (`Alice - #3B82F6`) and live element highlight rings directly inside the active canvas viewport. | Doc 3 / Doc 4 | HIGH / HIGH | No / `CNV-01`, `COL-02`| **M** / `8 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Canvas | **Ecosystem Pod** — Connected WebSocket peers display floating cursor tags; selecting node displays teammate name label on node border. |
| **`CNV-09`** | **Spatial 3D Viewport Navigation (`OrbitControls`):** 3D camera controller allowing users to pan, zoom, and rotate around multi-plane spatial computing interfaces (`Three.js / WebXR Orbiting`). | Doc 2 / Doc 7 | VISION / VISION | No / `CNV-01`, `PRJ-07`| **L** / `14 Days` | Not Started / **Sprint 12** | **`Future`** / Canvas | **Ecosystem Pod** — Mouse drag rotates 3D canvas viewport; depth planes (`z-index` depth) render with authentic spatial lighting. |
| **`CNV-10`** | **Autonomous Visual Regression Heatmap Overlay:** Background rendering comparator generating visual diff overlays highlighting unintended CSS shifts or visual regressions across viewport breakpoints. | Doc 4 / Doc 7 | VISION / HIGH | Yes (`Vision`) / `CNV-01`| **L** / `12 Days` | Not Started / **Sprint 12** | **`Future`** / Canvas | **Canvas Pod** — Auto-snapshot before/after commit; renders red overlay where pixel layout shifted by `> 2px` across breakpoints. |

---

# MODULE 5: AST ENGINE (`@dios/ast-core`) — 9 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`AST-01`** | **Unified JSONB AST Node Schema (`IASTNode`):** Standardized, version-controlled tree contract (`nodeId`, `type`, `props`, `styles`, `children`) representing clean Next.js/React DOM structures. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / None | **S** / `3 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / AST | **AST Pod** — Zod schema exported from `@dios/types`; validates all incoming/outgoing AST JSON payloads with zero type errors. |
| **`AST-02`** | **SWC / Babel TypeScript AST Parser & Serializer:** Fast TypeScript visitor engine (`SWC`) parsing raw JSX/TSX files into `ASTNode` trees and serializing mutated trees back into clean React code. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `AST-01` | **L** / `12 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / AST | **AST Pod** — `parseJSX(code)` outputs exact `IASTNode` tree; `serializeAST(tree)` outputs cleanly formatted Next.js 15 TSX in `< 50ms`. |
| **`AST-03`** | **Sub-Tree Structural Diffing & Patch Engine:** Precision mutation engine computing minimal delta patches (`ASTMutationPatch`) when elements are dragged on canvas or AI modifies a specific section. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `AST-02` | **M** / `8 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / AST | **AST Pod** — `computePatch(oldTree, newTree)` outputs exact node deltas (`add / remove / replace`); applies patch to state in `< 5ms`. |
| **`AST-04`** | **Zstd JSONB Compressed Database Storage (`project_versions`):** High-compression database storage engine compressing AST `JSONB` trees via `Zstd` before writing to PostgreSQL, backed by GIN `jsonb_path_ops` indexes. | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `AST-01`, `PRJ-01`| **M** / `6 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / AST | **AST Pod** — 500-node site AST compresses from 250KB to `< 35KB` in database; JSONB path queries execute in `< 10ms`. |
| **`AST-05`** | **Scoped Sub-Tree Extraction & Window Pruning:** Token optimization extractor slicing active `ASTNodeId` and direct parents/children for AI context (`never sending full 500-node site AST to LLM`). | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `AST-01` | **S** / `4 Days` | Not Started / **Sprint 1** | **`v0.5 Alpha`** / AST | **AST Pod** — `extractSubTree(ast, 'node-123')` returns exact 3-level parent/child window (`< 1,500 tokens`) with relative path map. |
| **`AST-06`** | **AST Node ID Last-Write-Wins Conflict Resolver:** Structural collision handler merging simultaneous canvas edits and Git commits by locking changes to unique `ASTNodeId` boundaries (`No file-level locking`). | Doc 6 / Doc 7 | HIGH / CRITICAL | No / `AST-03`, `PRJ-03`| **M** / `8 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / AST | **AST Pod** — Simultaneous edits to different section IDs merge cleanly without conflict; simultaneous edits to same node ID apply last timestamp. |
| **`AST-07`** | **Dynamic Import & Code-Splitting AST Transformer:** Automated compiler optimization scanning AST for heavy interactive nodes (`Carousels, Charts`) and injecting `next/dynamic` lazy imports automatically. | Doc 4 / Doc 6 | HIGH / HIGH | No / `AST-02` | **M** / `7 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / AST | **AST Pod** — Serialized code automatically wraps heavy client components in `const Chart = dynamic(() => import('./Chart'))`. |
| **`AST-08`** | **CRDT State Synchronization Contract (`IASTCollaborative`):** Extended AST node wrapper binding every `ASTNodeId` inside Yjs conflict-free replicated data structures (`CRDT`) for mathematical multi-editor convergence. | Doc 4 / Doc 7 | HIGH / CRITICAL | No / `AST-01` | **L** / `10 Days` | Not Started / **Sprint 12** | **`Future`** / AST | **AST Pod** — `IASTNode` converts cleanly to Yjs `Y.Map`; local mutations apply to CRDT document and emit sync updates without data corruption. |
| **`AST-09`** | **Self-Healing Syntax Tree Recovery & Repair Loop:** Compiler watchdog scanning corrupted user/AI AST inputs, repairing missing closing brackets or malformed props deterministically before crashing parser. | Doc 4 / Doc 7 | VISION / HIGH | No / `AST-02` | **M** / `8 Days` | Not Started / **Sprint 12** | **`Future`** / AST | **AST Pod** — Malformed JSON or broken JSX syntax caught by repair loop; auto-closes missing tags and logs warning without fatal UI crash. |

---

# MODULE 6: AI ORCHESTRATION (`@dios/ai`) — 11 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`AI-01`** | **Floating Natural Language Prompt Bar (`Cmd+K` Studio):** Bottom-docked AI input interface (`Cmd+K`) accepting multi-turn prompts with instant element targeting (`Targeting: #hero-heading`). | Doc 3 / Doc 4 | CRITICAL / HIGH | Yes (`Haiku`) / `CNV-01`| **S** / `5 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / AI | **AI Pod** — Pressing `Cmd+K` opens floating input drawer; selecting canvas node auto-populates `Target: #nodeId`; submits prompt via SSE. |
| **`AI-02`** | **Router Agent (`Claude 3.5 Haiku Intent Classifier`):** Ultra-fast intent classifier (`< 250ms`) routing prompts (`CREATE_SECTION`, `UPDATE_STYLE`, `REFACTOR_COPY`) and slicing target `ASTNodeId` windows. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | Yes (`Haiku`) / `AST-05`| **S** / `4 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / AI | **AI Pod** — Prompt classified in `< 250ms`; outputs exact `intent` and `targetNodeId` JSON payload; costs `$0.001 per classification`. |
| **`AI-03`** | **Unified Generator Agent (`Claude 3.7 Sonnet AST Engine`):** Primary structural layout and token styling generator streaming exact `ASTMutationPatch` payloads via Server-Sent Events (`SSE` streaming). | Doc 4 / Doc 6 | CRITICAL / CRITICAL | Yes (`Sonnet`)/ `AI-02`, `TKN-01`| **L** / `12 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / AI | **AI Pod** — Streams valid JSON patch in `2.0–3.5s`; strictly uses `tokens.json` design variables (`color.bg.primary`); zero hallucinated hex codes. |
| **`AI-04`** | **Deterministic Static Quality Gate (`Local TS Linters`):** Zero-AI-cost static analysis runner (`axe-core`, `DOMPurify`, `ESLint`) verifying contrast (`4.5:1`) and stripping XSS from patches in `< 10ms`. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `AST-03` | **M** / `6 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / AI | **AI Pod** — Patch runs through static gate before applying to AST; contrast failures (`< 4.5:1`) auto-adjusted; XSS `<script>` tags stripped. |
| **`AI-05`** | **Multi-Modal Image-to-Code Vision Converter:** Vision pipeline (`Claude 3.7 Vision / GPT-4o`) accepting wireframe screenshots or whiteboard photos and converting directly to clean AST layouts. | Doc 3 / Doc 4 | CRITICAL / HIGH | Yes (`Vision`) / `AI-03`, `TKN-02`| **M** / `8 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / AI | **AI Pod** — User drops PNG image into `Cmd+K` bar; vision model extracts structural layout and maps exact `tokens.json` color/spacing classes. |
| **`AI-06`** | **Reviewer & Self-Healing Loop (`Max 1 Retry Gate`):** Automated compiler verification agent catching SWC/React syntax errors and feeding stack trace back to Sonnet once before returning to user. | Doc 4 / Doc 6 | CRITICAL / HIGH | Yes (`Sonnet`)/ `AI-03`, `AI-04`| **M** / `6 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / AI | **AI Pod** — If generated patch throws SWC parser error, loop triggers 1 auto-retry with trace info (`"Fix unclosed tag"`); `99.9% reliability`. |
| **`AI-07`** | **Orchestration Expansion (`Layout, UX & Copy Specialists`):** Activation of specialized agents (`ORC-06 Layout`, `ORC-07 UX Specialist`, `ORC-08 Copywriter`) routed selectively on complex multi-turn prompts. | Doc 4 / Doc 7 | HIGH / HIGH | Yes (`Sonnet`)/ `AI-02` | **M** / `8 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / AI | **AI Pod** — Complex prompts (`"Redesign pricing with glassmorphism"`) trigger UX Specialist; generates premium micro-animations (`duration-200`). |
| **`AI-08`** | **Vector Memory & RAG Context Engine (`pgvector`):** PostgreSQL embedding retrieval engine (`pgvector + Zstd`) recalling past user style preferences (`"User prefers 8px rounded borders"`) during generation. | Doc 4 / Doc 6 | HIGH / CRITICAL | Yes (`Embed`) / `AI-03` | **L** / `10 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / AI | **AI Pod** — Past prompt corrections indexed in `pgvector`; cosine similarity query (`< 15ms`) injects top 3 user preferences into Sonnet system prompt. |
| **`AI-09`** | **Planner Agent Interface (`DeepSeek-R1 / o3 Wireframer`):** Advanced structural wireframe architect breaking massive multi-page site requests (`"Build an Airbnb clone"`) into progressive 5-step milestone DAGs. | Doc 4 / Doc 7 | HIGH / HIGH | Yes (`o3/R1`) / `AI-03` | **L** / `12 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / AI | **AI Pod** — High-level prompt outputs milestone breakdown UI (`Step 1: Header, Step 2: Hero...`); user approves step-by-step generation. |
| **`AI-10`** | **Automated Accessibility & Performance Remediation Agents:** Dedicated deep audit agents (`ORC-10 A11y`, `ORC-11 Perf`) scanning entire site AST and auto-injecting `aria-labels`, `loading="lazy"`, and skip links. | Doc 4 / Doc 7 | HIGH / HIGH | Yes (`Sonnet`)/ `AI-04` | **M** / `8 Days` | Not Started / **Sprint 12** | **`Future`** / AI | **AI Pod** — 1-Click "Optimize Site" triggers deep background scan; generates unified `ASTMutationPatch` fixing 100% of WCAG AA & Lighthouse errors. |
| **`AI-11`** | **Autonomous Multi-Agent Background Watchdog Loop:** Background agent monitoring live edge `*.dios.app` sites for broken third-party links or JS runtime errors, auto-committing repair patches. | Doc 4 / Doc 7 | VISION / VISION | Yes (`Sonnet`)/ `DEP-02` | **XL** / `18 Days` | Not Started / **Sprint 12** | **`Future`** / AI | **AI Pod** — External synthetic monitor pings site every 10 min; if HTTP 500 or JS error detected, Watchdog parses trace, fixes AST, and deploys patch. |

---

# MODULE 7: COMPONENTS (`@dios/components`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`CMP-01`** | **11 Built-In Core Component Specifications (`@dios/ast-core`):** Production-ready React 19 specs for `Hero`, `Navigation`, `Pricing Table`, `Feature Grid`, `Testimonial`, `FAQ`, `Contact`, `Footer`, `CTA`, `Blog`, and `Team`. | Doc 3 / Doc 4 | CRITICAL / CRITICAL | No / `AST-01`, `TKN-01`| **M** / `10 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Components | **AST Pod** — All 11 components coded in clean TSX; strictly consume `tokens.json` keys; render cleanly inside virtualized canvas. |
| **`CMP-02`** | **50 Hardcoded Obsidian Brand Presets (`/presets`):** Curated portfolio of 50 pre-compiled `tokens.json` brand templates (`Cyberpunk Dark`, `Fintech Clean`, `SaaS Modern`, `Editorial Luxury`) available at launch. | Doc 3 / Doc 6 | CRITICAL / HIGH | No / `TKN-01` | **S** / `5 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Components | **AST Pod** — All 50 brand kits stored in `/packages/tokens/src/presets/*.json`; 1-click theme switch applies exact color/font ratios across canvas. |
| **`CMP-03`** | **1-Click Component Insertion Drawer (`Slide-Over Browser`):** Visual component browser slide-over allowing users to drag and drop pre-built components directly onto canvas section split-points. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `CMP-01`, `CNV-01`| **S** / `4 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Components | **Canvas Pod** — Left-hand drawer displays searchable component categories; dragging item onto canvas inserts exact AST node at drop index. |
| **`CMP-04`** | **Interactive Component Prop Customizer & Variant Selector:** Automated prop variation selector (`variant="primary" | "outline" | "ghost"`) exposed directly inside right-hand Property Inspector panel. | Doc 3 / Doc 4 | HIGH / HIGH | No / `CMP-01`, `CNV-02`| **S** / `4 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Components | **Canvas Pod** — Selecting `PricingTable` node exposes `columns: 2 | 3 | 4` and `style: card | minimal` dropdowns; toggling prop updates AST instantly. |
| **`CMP-05`** | **Programmatic Component Directory (`/components/*` SSG):** Automated SSG generator rendering 10,000+ SEO-optimized landing pages targeting long-tail queries (`"Tailwind dark mode pricing table react"`). | Doc 5 / Doc 7 | HIGH / HIGH | No / `CMP-01` | **M** / `7 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Components | **Platform Pod** — Next.js SSG builds `/components/[category]/[slug]` pages with live interactive preview iframe and "Use Template" CTA button. |
| **`CMP-06`** | **Component License & Attribution Verification Engine:** Automated ingestion scanner running static checks on third-party marketplace components to ensure proper open-source or commercial license tags. | Doc 7 / Doc 6 | HIGH / MEDIUM | No / `CMP-01` | **S** / `4 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Components | **Ecosystem Pod** — Seller component submission requires explicit license selection (`MIT / Commercial`); license tag embedded in component AST metadata. |

---

# MODULE 8: DESIGN TOKENS (`@dios/tokens`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`TKN-01`** | **W3C `tokens.json` Schema Validation Engine (`Zod Law`):** Strict JSON schema parser binding all visual properties to W3C Design Tokens format (`color.bg.primary`, `space.8`, `font.heading.xl`). | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / None | **S** / `3 Days` | Not Started / **Sprint 0** | **`v0.5 Alpha`** / Design Tokens | **AST Pod** — Zod schema exported from `@dios/types`; enforces exact W3C token structure; rejects malformed token files with detailed trace errors. |
| **`TKN-02`** | **Real-Time Token-to-Tailwind v4 CSS Compiler (`< 5ms`):** High-speed compiler transforming `tokens.json` directly into native Tailwind CSS v4 variables and utility classes without page reloads. | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `TKN-01` | **M** / `7 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Design Tokens | **AST Pod** — Modifying `tokens.json` recompiles CSS stylesheet dynamically; canvas styling reflects changes in `< 5ms` without full iframe reload. |
| **`TKN-03`** | **Hardcoded Token Enforcement Rule Engine (`Zero Hex`):** Compiler validation rule blocking ad-hoc hex strings (`#123456`) or magic spacing numbers in user/AI edits, forcing mapping to nearest token variable. | Doc 3 / Doc 6 | CRITICAL / HIGH | No / `TKN-01` | **S** / `4 Days` | Not Started / **Sprint 2** | **`v0.5 Alpha`** / Design Tokens | **AST Pod** — If AI attempts to set `style.backgroundColor = '#10B981'`, compiler intercepts and maps to nearest token `color.accent.primary`. |
| **`TKN-04`** | **Interactive Theme Studio & Dark/Light Mode Inverter:** Visual design system editor allowing 1-click theme inversion (`Light -> Dark -> High Contrast`) while preserving harmonious WCAG contrast ratios. | Doc 3 / Doc 4 | CRITICAL / HIGH | No / `TKN-02` | **M** / `6 Days` | Not Started / **Sprint 5** | **`v1.0 Public`** / Design Tokens | **Canvas Pod** — Theme Studio UI displays primary/secondary color swatches; toggling "Dark Mode" auto-generates inverted color token scale cleanly. |
| **`TKN-05`** | **Bi-Directional Figma Token Sync Bridge (`@dios/figma`):** Figma plugin (`@dios/figma-sync`) enabling 2-way import/export of `tokens.json` and local variable collections directly between Figma and DIOS. | Doc 3 / Doc 4 | HIGH / CRITICAL | No / `TKN-01` | **L** / `12 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Design Tokens | **Ecosystem Pod** — Figma plugin authenticates via Clerk; clicking "Sync Tokens" pushes Figma variables directly to active project `tokens.json`. |
| **`TKN-06`** | **Spatial Computing Token Extension Math (`Depth & Lux`):** Extended token schema introducing 3D spatial variables (`space.depth.8`, `light.lux.high`, `material.roughness.low`) for WebXR viewports. | Doc 2 / Doc 7 | VISION / VISION | No / `TKN-01` | **M** / `8 Days` | Not Started / **Sprint 12** | **`Future`** / Design Tokens | **Ecosystem Pod** — Token compiler converts spatial tokens into Three.js material properties (`MeshStandardMaterial.roughness = 0.2`) cleanly. |

---

# MODULE 9: DEPLOYMENT (`@dios/deploy`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`DEP-01`** | **1-Click Static HTML/CSS/JS Edge Compiler (`Next.js /out`):** Next.js server action compiling active AST and Tailwind tokens into standalone static HTML/JS chunks (`/out/*`) in `< 2.5 seconds`. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `AST-02`, `TKN-02`| **L** / `10 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Deployment | **Platform Pod** — Clicking "Publish" runs static build pipeline; outputs clean production bundle with zero DIOS runtime dependencies. |
| **`DEP-02`** | **Cloudflare R2 + Edge KV Anycast Publisher (`*.dios.app`):** Direct HTTP PUT publisher uploading compiled bundles to Cloudflare R2 (`$0 egress`) and mapping `slug.dios.app` pointers inside Edge KV. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `DEP-01` | **M** / `6 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Deployment | **Platform Pod** — Static files upload to R2 bucket in `< 1.5s`; Edge KV pointer updates (`acme -> /v123/`); site accessible globally via Edge network. |
| **`DEP-03`** | **Instant Edge Rollback Pointer Controller (`< 1s`):** 1-click disaster recovery mechanism repointing active Cloudflare Edge KV release key (`active_version_id`) to previous snapshot in `< 1s`. | Doc 4 / Doc 6 | HIGH / HIGH | No / `DEP-02` | **S** / `3 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Deployment | **Platform Pod** — Clicking "Rollback" on deployment history updates Edge KV release key instantly; live site reverts to previous version without rebuild. |
| **`DEP-04`** | **Custom Domain SSL & DNS Verification Router (`CNAME`):** Edge routing worker verifying `CNAME / A` records (`acme.com -> cname.dios.app`) and provisioning automated Cloudflare Universal SSL certificates. | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `DEP-02` | **M** / `8 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Deployment | **Platform Pod** — User enters custom domain; UI displays required CNAME record; once DNS verified, Cloudflare SSL provisioned automatically. |
| **`DEP-05`** | **Automated Edge Image & Asset Optimization (`Next/Image`):** Edge pipeline intercepting image requests (`/cdn-cgi/image/*`), converting raw assets to WebP/AVIF, and serving responsive srcset resolutions. | Doc 4 / Doc 6 | HIGH / HIGH | No / `DEP-02` | **M** / `6 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Deployment | **Platform Pod** — Uploaded hero image (`10MB PNG`) automatically compressed and served as `80KB AVIF` at exact viewport dimensions via Edge cdn. |
| **`DEP-06`** | **Multi-Region Air-Gapped AWS VPC Sharded Deployer:** Enterprise deployment automation (`Terraform / Pulumi`) spinning up single-tenant DIOS runtime shards inside customer AWS/GCP cloud environments. | Doc 4 / Doc 7 | HIGH / CRITICAL | No / `DEP-01` | **XL** / `18 Days` | Not Started / **Sprint 12** | **`Future`** / Deployment | **Enterprise Pod** — Terraform script provisions EKS/RDS shard inside private customer VPC; runs full DIOS suite with zero outbound public internet access. |

---

# MODULE 10: GIT MONOREPO SYNC (`@dios/git`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`GIT-001`** | **GitHub App Repository Provisioner & Linker:** 1-click GitHub App integration authorizing `@moolox/git` to create repositories or link to existing Next.js App Router branches (`main / staging`). | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `AUTH-007` | **M** / `6 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Git | **Platform Pod** — Clicking "Connect GitHub" opens the repository selection picker; webhooks are installed; the target branch is linked to the active project slug. |
| **`GIT-02`** | **AST-to-Next.js 15 App Router Code Exporter (`Clean Code`):** Compiler exporting active AST and `tokens.json` to human-readable Next.js 15 App Router code (`/src/app/*`) with zero DIOS runtime lock-in. | Doc 3 / Doc 6 | CRITICAL / CRITICAL | No / `AST-02`, `TKN-02`| **L** / `12 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Git | **AST Pod** — Exported code passes `npm run build` cleanly locally; code uses standard React server/client components and clean Tailwind classes. |
| **`GIT-03`** | **Inngest Background Git Push & Commit Formatter:** Background event worker (`Inngest`) bundling code exports and pushing atomic commits (`feat(dios): update hero section [skip ci]`) to user repository. | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `GIT-02` | **M** / `6 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Git | **Platform Pod** — Canvas save triggers background job; commit pushed to user's branch in `< 4 seconds` with clean diff history. |
| **`GIT-04`** | **Incoming GitHub Webhook Bidirectional Code Puller:** Webhook listener (`push event`) detecting developer code edits pushed from VS Code, running SWC parser, and updating canvas AST in `< 3 seconds`. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `GIT-01`, `AST-02`| **L** / `10 Days` | Not Started / **Sprint 4** | **`v1.0 Public`** / Git | **AST Pod** — Developer runs `git push` from terminal; webhook received; SWC parses mutated TSX; canvas AST reloads seamlessly. |
| **`GIT-05`** | **AST Node ID Structural Merge & Conflict Resolver:** Structural collision handler merging simultaneous canvas edits and Git commits by locking changes to unique `ASTNodeId` boundaries (`Last-write-wins`). | Doc 6 / Doc 7 | HIGH / CRITICAL | No / `GIT-04`, `AST-06`| **M** / `8 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Git | **AST Pod** — Simultaneous edits to different section IDs merge cleanly; conflicting edits to same node ID apply latest timestamp without breaking tree. |
| **`GIT-06`** | **External CI/CD Pipeline & GitHub Action Validator (`@dios/ci`):** Custom GitHub Action package (`@dios/action`) allowing developers to run DIOS design token validation and accessibility linters directly inside their PR pipelines. | Doc 4 / Doc 7 | HIGH / HIGH | No / `GIT-01` | **M** / `6 Days` | Not Started / **Sprint 11** | **`Platform`** / Git | **Platform Pod** — Developer opens pull request in GitHub; `@dios/action` runs static checks; blocks PR merge if design token violations found. |

---

# MODULE 11: MARKETPLACE (`@dios/marketplace`) — 7 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`MKT-01`** | **Creator Marketplace Domain Service Core (`IMarketplace`):** Standardized service contract (`getListing()`, `publishComponent()`, `verifyLicense()`) decoupling UI presentation from underlying data stores. | Doc 3 / Doc 7 | CRITICAL / HIGH | No / None (Base Core) | **S** / `4 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Marketplace | **Ecosystem Pod** — Service layer mounted cleanly inside `@dios/marketplace`; returns built-in components in `v1.0` and seller items in `v2.0`. |
| **`MKT-02`** | **Component & Brand Kit Registry Schema (`@dios/db`):** Extensible Drizzle ORM tables (`marketplace_items`, `seller_accounts`, `item_reviews`) storing component ASTs, preview URLs, and price tags. | Doc 5 / Doc 7 | HIGH / HIGH | No / `WS-01` | **S** / `4 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Marketplace | **Ecosystem Pod** — All marketplace tables preserved in schema from Day 1 (`is_active = false`); activated cleanly in `v2.0` without DB migration. |
| **`MKT-03`** | **Stripe Connect Automated Seller Revenue Split (`80/20`):** Automated revenue-sharing pipeline executing 80% creator payouts and retaining 20% platform commission upon every component purchase. | Doc 5 / Doc 7 | CRITICAL / CRITICAL | No / `MKT-02`, `BIL-01`| **L** / `10 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Marketplace | **Ecosystem Pod** — Buyer completes Stripe checkout; 80% transferred instantly to seller's connected Express account; item AST unlocked to buyer. |
| **`MKT-04`** | **Automated Component Security & Quality Sandboxing Gate:** Automated ingestion scanner running static quality gates (`AI-04`) on submitted seller ASTs to verify zero XSS or external network hooks before listing. | Doc 6 / Doc 7 | CRITICAL / HIGH | No / `AI-04`, `MKT-02`| **M** / `8 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Marketplace | **Ecosystem Pod** — Seller submits component; static scanner audits AST tree; if external script or `<iframe >` tag found, submission rejected automatically. |
| **`MKT-05`** | **Public Creator Storefronts & Star Rating Matrix:** Public-facing creator profile pages (`dios.app/@creator`) showcasing verified sales counts, customer review stars (`4.9/5.0`), and live component demos. | Doc 3 / Doc 7 | HIGH / HIGH | No / `MKT-02` | **M** / `6 Days` | Not Started / **Sprint 8** | **`v2.0 Ecosystem`** / Marketplace | **Ecosystem Pod** — Creator profile renders clean storefront with component cards; verified purchasers can leave 1-5 star ratings and reviews. |
| **`MKT-06`** | **Agency White-Label Template Distribution Network:** Private marketplace tier allowing verified agencies to publish proprietary template libraries (`Agency Kits`) accessible exclusively to their invited clients. | Doc 5 / Doc 7 | HIGH / HIGH | No / `MKT-01`, `WS-06`| **M** / `8 Days` | Not Started / **Sprint 11** | **`Platform`** / Marketplace | **Ecosystem Pod** — Agency marks component as `Private - Client Only`; item visible only to authenticated workspaces linked to agency organization. |
| **`MKT-07`** | **AI-Powered Component Remixing & Style Adaptation Engine:** Intelligent marketplace bridge (`Claude 3.7`) allowing users to click "Adapt to My Brand" on any marketplace component, matching local `tokens.json`. | Doc 3 / Doc 7 | VISION / HIGH | Yes (`Sonnet`)/ `MKT-01`, `TKN-02`| **L** / `12 Days` | Not Started / **Sprint 12** | **`Future`** / Marketplace | **AI Pod** — User clicks "Adapt"; AI scans target item AST and rewrites exact color/font tokens to match user's active brand kit in `< 3s`. |

---

# MODULE 12: BILLING & SUBSCRIPTIONS (`@dios/billing`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`BIL-01`** | **Stripe Billing Webhook Core & Subscription Synchronization:** Core Stripe webhook listener (`checkout.session.completed`, `invoice.paid`) syncing active subscription status to `subscriptions` table. | Doc 4 / Doc 5 | CRITICAL / CRITICAL | No / `WS-01` | **S** / `4 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Billing | **Platform Pod** — Webhook endpoint (`/api/webhooks/stripe`) verifies signature; updates `workspaces.plan` instantly on successful checkout. |
| **`BIL-02`** | **Free Tier Usage Gate (`*.dios.app` Badge & 10 AI Credits):** Subscription enforcement checking free plan quotas (`Max 10 AI credits/mo`, mandatory `"Built with DIOS"` edge banner on published sites). | Doc 5 / Doc 6 | CRITICAL / HIGH | No / `BIL-01`, `DEP-02`| **S** / `4 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Billing | **Platform Pod** — Free user publishing site gets edge banner injected; AI credit counter blocks prompts after 10 turns without upgrade modal. |
| **`BIL-03`** | **Pro Tier Subscription Gate (`$29/mo` - Git Export & Custom Domain):** Pro plan feature gating unlocking bidirectional GitHub monorepo export (`GIT-01`), custom domain SSL (`DEP-04`), and 500 monthly AI credits. | Doc 5 / Doc 6 | CRITICAL / CRITICAL | No / `BIL-01` | **S** / `4 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Billing | **Platform Pod** — Clicking "Connect GitHub" on Free plan opens Stripe checkout modal; active Pro workspace unlocks full Git export and custom DNS. |
| **`BIL-04`** | **Agency Tier Subscription Gate (`$299/mo` - White-Label Portal):** Agency plan feature gating unlocking custom brand portal (`build.agency.com`), unlimited client editor seats, and 5,000 monthly AI credits. | Doc 5 / Doc 6 | CRITICAL / HIGH | No / `BIL-01`, `WS-06`| **S** / `4 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Billing | **Platform Pod** — Agency subscription activates `@dios/agency` custom portal domain and client seat invitation tools. |
| **`BIL-05`** | **Metered AI Credit Top-Up Checkout Bridge (`$10 / 1,000 Credits`):** On-demand Stripe Checkout flow allowing Pro and Agency workspaces to purchase additional AI credit packs when their monthly quota runs low. | Doc 5 / Doc 6 | HIGH / HIGH | No / `BIL-01`, `AUTH-03`| **S** / `4 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Billing | **Platform Pod** — User clicks "Buy Credits"; Stripe checkout processes $10 payment; webhook increments `workspaces.ai_credits_limit` by `+1,000`. |
| **`BIL-06`** | **Enterprise Custom Invoicing & Purchase Order (`PO`) Gateway:** Manual billing tier allowing enterprise contracts ($30K–$100K ACV) to bypass Stripe credit cards in favor of Net-30 invoicing and custom seats. | Doc 5 / Doc 6 | HIGH / MEDIUM | No / `BIL-01`, `ENT-01`| **S** / `4 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Billing | **Enterprise Pod** — Admin flags workspace as `Enterprise Invoiced`; bypasses automated Stripe lockouts; billing managed via annual invoice schedule. |

---

# MODULE 13: ANALYTICS & OBSERVABILITY (`@dios/analytics`) — 5 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`ANA-01`** | **OpenTelemetry (`OTel`) Distributed Trace & Span Injection:** Core telemetry middleware injecting `trace_id` and `span_id` across Next.js API routes, Inngest workers, and LLM SSE streams (`Grafana LGTM stack`). | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `AUTH-01` | **M** / `6 Days` | Not Started / **Sprint 3** | **`v0.5 Alpha`** / Analytics | **Platform Pod** — All HTTP requests and async workers emit OTel spans; Grafana dashboard renders exact latency breakdown per system tier. |
| **`ANA-02`** | **Customer Site Analytics & Core Web Vitals Script Injector:** Lightweight privacy-first analytics script (`< 1KB`) automatically injected into published edge sites (`*.dios.app`) tracking page views and LCP vitals. | Doc 3 / Doc 4 | HIGH / HIGH | No / `DEP-01` | **S** / `4 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Analytics | **Platform Pod** — Published site reports page views and LCP/CLS metrics back to `/api/analytics/collect`; rendered inside workspace dashboard. |
| **`ANA-03`** | **AI Cost & Token Consumption Dashboard Matrix:** Granular telemetry dashboard displaying exact token usage (`Haiku vs Sonnet vs Vision`), cost per prompt turn (`$0.085 avg`), and model latency trends. | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `ANA-01`, `AUTH-03`| **S** / `5 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Analytics | **AI Pod** — Admin dashboard displays exact cost breakdown per project and user; alerts when P95 AI generation latency exceeds `3.5 seconds`. |
| **`ANA-04`** | **Weekly Active Editor (`WAE`) & Retention Funnel Telemetry:** Product-led growth (`PLG`) tracking engine monitoring key conversion milestones (`Signup -> First Save -> First Git Push -> Upgrade to Pro`). | Doc 5 / Doc 6 | HIGH / HIGH | No / `ANA-01` | **S** / `4 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Analytics | **Platform Pod** — PostHog / Mixpanel events emitted cleanly on core user actions; internal founder dashboard tracks exact WAE retention cohorts. |
| **`ANA-05`** | **Enterprise Compliance & Audit Log Export Bridge (`SIEM`):** Automated export bridge allowing enterprise security teams to stream workspace audit logs (`audit_logs` table) directly to Datadog, Splunk, or AWS S3. | Doc 5 / Doc 6 | HIGH / HIGH | No / `WS-05`, `ENT-01`| **M** / `6 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Analytics | **Enterprise Pod** — Enterprise admin configures SIEM webhook URL; all audit events push asynchronously via Inngest retry queue in `< 10 seconds`. |

---

# MODULE 14: ENTERPRISE & GOVERNANCE (`@dios/enterprise`) — 8 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`ENT-01`** | **Enterprise Organization Hierarchy (`enterprise_orgs`):** Multi-tiered tenancy hierarchy (`Org -> Department -> Workspace -> Project`) enabling centralized billing, audit logs, and global security policies across teams. | Doc 5 / Doc 7 | CRITICAL / CRITICAL | No / `WS-01` | **M** / `8 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Enterprise | **Enterprise Pod** — Schema table active from Day 1 (`is_active = false`); activated cleanly; parent org policies override child workspace settings. |
| **`ENT-02`** | **Automated Continuous SOC2 Type II Monitoring Engine:** Integration with Drata/Vanta automating background check tracking, AWS least-privilege IAM evidence, and continuous compliance verification logs. | Doc 5 / Doc 6 | CRITICAL / HIGH | No / None (Operational) | **S** / `4 Days` | Not Started / **Sprint 6** | **`v1.0 Public`** / Enterprise | **Platform Pod** — Continuous compliance agent running against infrastructure; zero failing SOC2 controls reported across all cloud endpoints. |
| **`ENT-03`** | **European GDPR Data Residency Shard (`eu-west-1 Dublin`):** Infrastructure isolation routing European workspace ASTs and database transactions strictly to AWS Dublin / Cloudflare EU Anycast edge nodes. | Doc 5 / Doc 6 | CRITICAL / CRITICAL | No / `DEP-02`, `ENT-01`| **L** / `12 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Enterprise | **Enterprise Pod** — Org setting `residency: 'EU'` forces PostgreSQL poolers and edge publishers to read/write exclusively within EU borders (`GDPR DPA compliant`). |
| **`ENT-04`** | **SCIM 2.0 Automated User Provisioning & Deprovisioning:** Enterprise identity bridge (`Clerk SCIM`) automating employee onboarding and instant access revocation when staff leave corporate directories (`Okta`). | Doc 5 / Doc 6 | HIGH / CRITICAL | No / `AUTH-05`, `ENT-01`| **M** / `8 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Enterprise | **Enterprise Pod** — SCIM webhook endpoints handle user creation and deletion; employee terminated in Okta loses DIOS workspace access instantly. |
| **`ENT-05`** | **Customer-Managed Encryption Keys (`CMEK` / AWS KMS):** Data security bridge encrypting enterprise `project_versions.ast_tree` payloads using dedicated customer-controlled AWS KMS keys before DB write. | Doc 5 / Doc 6 | HIGH / CRITICAL | No / `AST-04`, `ENT-01`| **L** / `10 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Enterprise | **Enterprise Pod** — Admin supplies AWS KMS ARN; AST JSONB encrypted via envelop encryption; revoking KMS key renders DB data unreadable to DIOS. |
| **`ENT-06`** | **HIPAA Protected Health Information (`PHI`) Compliance Shield:** Strict compliance toggle stripping patient identifiers, disabling third-party external CDN scripts, and enforcing BAA agreements across AWS/Neon. | Doc 5 / Doc 6 | HIGH / HIGH | No / `ENT-01` | **M** / `6 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Enterprise | **Enterprise Pod** — HIPAA mode active (`is_hipaa_enforced = true`); blocks external image URLs and logs 100% of PHI field access to immutable audit log. |
| **`ENT-07`** | **Enterprise Custom Domain White-Label Admin Console:** Centralized control dashboard allowing enterprise IT administrators to manage custom SSO domains, force MFA, and review organization-wide credit limits. | Doc 5 / Doc 7 | HIGH / MEDIUM | No / `ENT-01`, `AUTH-05`| **M** / `6 Days` | Not Started / **Sprint 10** | **`Enterprise`** / Enterprise | **Enterprise Pod** — IT admin logs into `/enterprise/admin`; configures mandatory 2FA and reviews active seats across all departmental workspaces. |
| **`ENT-08`** | **Air-Gapped Multi-Region Shard Synchronization Bridge:** Replication controller syncing custom design tokens and brand kits across isolated, air-gapped single-tenant enterprise VPC shards securely. | Doc 4 / Doc 7 | VISION / CRITICAL | No / `DEP-06`, `ENT-01`| **XL** / `16 Days` | Not Started / **Sprint 12** | **`Future`** / Enterprise | **Enterprise Pod** — Encrypted sync bundle exported from central org repo and imported cleanly into air-gapped AWS VPC without network exposure. |

---

# MODULE 15: PLUGINS & WORKERS (`@dios/plugins`) — 6 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`PLG-01`** | **Plugin Extension Hook Abstraction (`IPluginHost`):** Internal event emitter interface exposing clean lifecycle hooks (`onASTMutate`, `onTokenChange`, `onBeforeDeploy`) inside core compilers right from Day 1. | Doc 4 / Doc 7 | CRITICAL / CRITICAL | No / `AST-01`, `TKN-01`| **S** / `4 Days` | Not Started / **Sprint 7** | **`v1.5 Polish`** / Plugins | **AST Pod** — Hook interfaces defined in `@dios/types`; core AST/Token compilers emit lifecycle events cleanly without performance overhead. |
| **`PLG-02`** | **Zero-DOM Web Worker Sandbox Engine (`WorkerGlobalScope`):** Secure runtime container executing third-party plugin JS inside isolated `WorkerGlobalScope` with zero access to `window`, `document`, or `fetch`. | Doc 4 / Doc 6 | CRITICAL / CRITICAL | No / `PLG-01` | **L** / `12 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Plugins | **Ecosystem Pod** — Third-party plugin code runs inside isolated Web Worker; attempts to access `window.location` or `fetch('evil.com')` throw sandbox errors. |
| **`PLG-03`** | **Typed `postMessage` RPC Communication Bridge:** High-speed message passing protocol allowing sandboxed workers to request specific AST sub-tree mutations (`{ action: 'AST_MUTATE', payload: patch }`). | Doc 4 / Doc 6 | CRITICAL / HIGH | No / `PLG-02`, `AST-03`| **M** / `8 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Plugins | **Ecosystem Pod** — Sandboxed worker emits RPC request; host validates permission manifest (`manifest.json`); applies patch to AST in `< 10ms`. |
| **`PLG-04`** | **Plugin Manifest Scope Engine (`manifest.json`):** Granular permission system requiring plugins to declare exact scopes (`['read:ast', 'write:tokens']`) verified prior to worker mounting. | Doc 4 / Doc 7 | HIGH / HIGH | No / `PLG-02` | **S** / `4 Days` | Not Started / **Sprint 9** | **`v2.0 Ecosystem`** / Plugins | **Ecosystem Pod** — Plugin manifest parsed on load; UI displays permission prompt (`"Plugin wants to edit your Design Tokens"`); unpermitted actions blocked. |
| **`PLG-05`** | **Public Developer SDK Package (`@dios/plugin-sdk`):** Fully typed npm package (`@dios/plugin-sdk`) with comprehensive documentation, CLI scaffolding tools, and local mock testing environments. | Doc 4 / Doc 7 | HIGH / HIGH | No / `PLG-03` | **M** / `8 Days` | Not Started / **Sprint 11** | **`Platform`** / Plugins | **Ecosystem Pod** — `npm install @dios/plugin-sdk` provides full TypeScript autocompletion for RPC actions and mock testing harnesses. |
| **`PLG-06`** | **Autonomous AI Plugin Generator & Scaffolding Agent:** Specialized prompt agent capable of generating complete, sandboxed third-party plugins (`manifest.json + worker.ts`) from natural language prompts. | Doc 3 / Doc 7 | VISION / HIGH | Yes (`Sonnet`)/ `PLG-05` | **L** / `12 Days` | Not Started / **Sprint 12** | **`Future`** / Plugins | **AI Pod** — User types `"Create a plugin that converts all images to grayscale on deploy"`; AI generates clean, secure Web Worker bundle. |

---

# MODULE 16: DEVELOPER SDK & PLATFORM (`@dios/sdk`) — 7 FEATURES

| Feature ID | Feature Name & Detailed Description | Source Doc | Biz / Eng Value | AI / Tech Dep | Complexity / Effort | Status & Sprint | Release & Module | Owner & Definition of Done |
|:---|:---|:---|:---:|:---:|:---:|:---:|:---:|:---|
| **`SDK-01`** | **Public Headless REST & tRPC API Endpoints (`/api/v1/*`):** Public programmatic endpoints allowing developers to query project ASTs, trigger AI generation turns, and initiate site deployments from external servers. | Doc 4 / Doc 7 | CRITICAL / CRITICAL | No / `AUTH-06`, `PRJ-01`| **M** / `8 Days` | Not Started / **Sprint 11** | **`Platform`** / SDK | **Platform Pod** — External `curl -H "Authorization: Bearer sk_live_*" https://dios.app/api/v1/projects` returns exact project metadata and AST tree cleanly. |
| **`SDK-02`** | **Official JavaScript / TypeScript Client Library (`@dios/sdk`):** Official npm client wrapper providing typed methods (`dios.projects.create()`, `dios.ai.generate()`) with automated retry logic and error handling. | Doc 4 / Doc 7 | HIGH / HIGH | No / `SDK-01` | **M** / `8 Days` | Not Started / **Sprint 11** | **`Platform`** / SDK | **Platform Pod** — `npm install @dios/sdk` provides 100% type coverage across all API endpoints; exact error codes (`DiosAuthenticationError`) thrown cleanly. |
| **`SDK-03`** | **Command Line Toolchain (`dios init / dios deploy`):** Official CLI tool (`@dios/cli`) enabling developers to initialize new local monorepos, pull latest `tokens.json`, and trigger edge deploys directly from terminal. | Doc 4 / Doc 7 | HIGH / HIGH | No / `SDK-02` | **M** / `8 Days` | Not Started / **Sprint 11** | **`Platform`** / SDK | **Platform Pod** — Running `npx @dios/cli@latest init` scaffolds clean Next.js 15 app locally linked to active DIOS project slug and git branch. |
| **`SDK-04`** | **Headless AI Site Generation Pipeline (`Automated Agencies`):** Programmatic batch generation loop allowing agencies to pass CSV/JSON customer lists (`1,000 businesses`) to `@dios/sdk` and generate 1,000 unique sites. | Doc 5 / Doc 7 | HIGH / CRITICAL | Yes (`Sonnet`)/ `SDK-02`, `AI-03`| **L** / `12 Days` | Not Started / **Sprint 12** | **`Future`** / SDK | **Ecosystem Pod** — Batch script invokes `dios.ai.generateBatch(customerArray)`; generates and deploys 1,000 unique sites to edge in `< 45 minutes`. |
| **`SDK-05`** | **Custom Webhook Dispatcher & Event Subscription Engine:** Webhook configuration console allowing developers to subscribe to real-time DIOS events (`project.saved`, `deploy.live`, `ai.completed`) via HTTP POST. | Doc 4 / Doc 7 | HIGH / HIGH | No / `SDK-01`, `INF-02`| **M** / `6 Days` | Not Started / **Sprint 12** | **`Future`** / SDK | **Platform Pod** — Admin enters destination webhook URL (`https://app.acme.com/webhook`); Inngest worker dispatches HMAC-signed payload on event. |
| **`SDK-06`** | **Figma & Adobe XD Programmatic Export SDK Bridge:** Low-level SDK adapter allowing third-party design tools to convert proprietary vector canvas structures directly into DIOS `ASTNode` JSON formats. | Doc 3 / Doc 7 | VISION / HIGH | No / `SDK-02`, `AST-01`| **L** / `14 Days` | Not Started / **Sprint 12** | **`Future`** / SDK | **Ecosystem Pod** — External design tool imports `@dios/sdk/adapters/figma`; converts vector shapes into exact layout boxes with `99% visual fidelity`. |
| **`SDK-07`** | **Spatial Computing / WebXR Headless 3D SDK Bridge:** Specialized SDK module (`@dios/sdk/spatial`) exposing spatial canvas matrices (`position3D`, `rotation3D`, `scale3D`) for Unity and Unreal Engine integrations. | Doc 2 / Doc 7 | VISION / VISION | No / `SDK-02`, `CNV-09`| **XL** / `18 Days` | Not Started / **Sprint 12** | **`Future`** / SDK | **Ecosystem Pod** — Unity runtime imports SDK spatial JSON; renders 3D WebXR UI layout directly inside VR headset viewports with zero dropped frames. |

---

*— End of File 2 (Canonical Master Feature Registry — 112 Features Deduplicated across 16 Modules) —*

---

# FINAL PRE-GA TRUST & VALIDATION ADDENDUM — 9 FEATURES

| Feature ID | Feature Name & Detailed Description | Source | Biz / Eng Value | Dependencies | Complexity | Status & Sprint | Module mapping | Owner & Definition of Done |
|:---|:---|:---|:---:|:---|:---:|:---:|:---|:---|
| **`PRJ-008`** | **Production Project Persistence and Save Transaction:** Authenticated, type-safe transaction persisting AST, tokens, active version, and optimistic version atomically. | `Moolox_2031.md` §12; `KNOWN_GAP.md` | CRITICAL / CRITICAL | `PRJ-001..004`, `CORE-002`, `AUTH-002`, `DB-001` | **M** | Not Started / **Sprint 5** | Projects + Core + DB | **Platform Pod** — stale save returns `409`; cross-tenant access fails; refresh restores exact committed hash; Playwright create→edit→save→reload passes. |
| **`GIT-009`** | **Brownfield Repository Onboarding and Compatibility Report:** Read-only analysis of a linked existing Next.js repository, identifying supported files/components, unsupported constructs, parse failures, dependencies, framework risk, tokens, and remediation. | `Moolox_2031.md` §§7,12,20 | CRITICAL / CRITICAL | `AUTH-007`, `GIT-001..004`, `AST-002`, `TKN-001` | **L** | Not Started / **Sprint 5** | Git + AST + Web | **AST Pod** — analysis never mutates the repository; editing remains disabled until explicit report acceptance; report is versioned and reproducible. |
| **`AST-011`** | **Safe Editability and Confidence Map:** Enforced classification of repository regions as `editable`, `review-required`, or `read-only`, including reasons and confidence. | `Moolox_2031.md` §§7,10,12 | CRITICAL / CRITICAL | `GIT-009`, `AST-002..003`, `AIS-003`, `CNV-001..004` | **L** | Not Started / **Sprint 5** | AST + Canvas + AI | **AST Pod** — canvas and AI enforce classifications; unsupported regions cannot be silently rewritten or included in patches. |
| **`CHG-001`** | **Semantic Change Object and Minimal-Diff Contract:** Immutable aggregate linking intent, base SHA/version, semantic IDs, AST patch, source diff, checks, actor, review, deployment, and outcome references. | `Moolox_2031.md` §§7,11,12 | CRITICAL / CRITICAL | `AST-003`, `AST-011`, `GIT-002..004`, `PRJ-008`, `WS-005` | **L** | Not Started / **Sprint 5** | Existing Project/AST/Git/Analytics contracts; no new module | **AST Pod** — unrelated file/node mutation count is zero; unchanged structures remain stable; every state transition is audited. |
| **`REV-001`** | **Designer-Readable Visual Pull-Request Review:** Dedicated branch/PR containing source diff, semantic summary, responsive before/after, checks, risk, and approve/reject workflow. | `Moolox_2031.md` §§7,10,12 | CRITICAL / HIGH | `CHG-001`, `GIT-003..005`, `CNV-003`, `DEP-002..003` | **L** | Not Started / **Sprint 6** | Git + Canvas + Web | **Canvas Pod** — rejection leaves target branch unchanged; merge SHA reconciles exactly to active project; unsafe conflicts pause. |
| **`PRV-001`** | **Analytics, AI-Memory, and Learning Consent Controls:** Explicit purpose/retention consent, withdrawal, memory inspection/deletion, and telemetry redaction. | `Moolox_2031.md` §§12,17 | CRITICAL / CRITICAL | `AUTH-002`, `ANA-001`, `WS-005` | **M** | Not Started / **Sprint 6** | Auth + Analytics + AI | **Platform Pod** — analytics/learning off by default until consent; withdrawal stops new collection; source, prompts, secrets, and raw assets never enter ordinary telemetry. |
| **`BIL-007`** | **Canonical Pricing, Billing Lifecycle, and AI-Economics Gate:** One approved pricing/credit model, complete signed Stripe lifecycle, idempotent entitlements, and workload gross-margin guardrails. | `Moolox_2031.md` §§12,24 | CRITICAL / HIGH | `BIL-001..003`, `AUTH-003`, `ANA-001` | **M** | Not Started / **Sprint 6** | Billing + Analytics | **Platform Pod** — checkout, invoice, proration, retry, cancellation, refund, and entitlement reconciliation pass; approved workloads stay above the agreed gross-margin floor. |
| **`TST-002`** | **Repository Round-Trip Corpus and GA Golden-Journey Certification:** Publicly reproducible proof across real repositories and complete user journeys. | `Moolox_2031.md` §§12,15,23 | CRITICAL / CRITICAL | `TST-001`, `GIT-009`, `AST-011`, `CHG-001`, `REV-001`, `PRJ-008` | **L** | Not Started / **Sprint 6** | Cross-cutting Test Assurance | **Quality Pod** — at least 10 non-demo repos × 10 cycles; supported semantic hashes preserved; unrelated diffs zero; failures categorized; signup→live and repo→safe PR journeys pass. |
| **`VAL-001`** | **Paid Design-Partner and Willingness-to-Pay Release Gate:** Evidence-based operating gate for qualified real-repository usage and payment. | `Moolox_2031.md` §§12,15,22,23 | CRITICAL / HIGH | `GIT-009`, `REV-001`, `BIL-007`, `TST-002` | **M** | Not Started / **Sprint 6** | Product/Founder operating workstream | **Founder/Product** — 5–10 qualified partners, at least 3 paid, ≥90% merge-without-manual-repair for supported changes, measured first-safe-PR time, and explicit GA decision. |

## Binding sprint reassignments

| Feature | Canonical sprint |
|:---|:---:|
| `TKN-004`, `CMP-003..004`, `CNV-005` | `5` |
| `GIT-005`, `AI-006`, `DEP-004`, `BIL-003`, `ANA-002` | `6` |
| `AI-005`, `AI-007`, `CNV-006` | `7` |
| `CMP-002`, `PRJ-005`, `BIL-004` | `8` |
| `ENT-001..002` | `10` |

Initial certification of `MIG-001`, `BKP-001`, `SEC-001..002`, `GIT-007..008`, `OPS-001`, `TST-001`, and `A11Y-001` occurs in Sprint 6; Sprint 7H recertifies and expands them.
