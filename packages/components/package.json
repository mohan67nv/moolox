{
  "name": "@moolox/components",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint pass'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@moolox/ast-core": "workspace:*",
    "@moolox/tokens": "workspace:*",
    "@moolox/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
